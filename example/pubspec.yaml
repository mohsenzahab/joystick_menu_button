name: joystick_menu_button_example
description: Example app for joystick_menu_button package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  joystick_menu_button:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true